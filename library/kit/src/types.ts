export enum ESize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

export enum EVariant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  GHOST = 'ghost',
}

export enum EMode {
  DEFAULT = 'default',
  SUCCESS = 'success',
  DANGER = 'danger',
}

export enum EAlign {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center',
}
